import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  section: {
    marginBottom: 24,
  },
  sectionTitle: {
    color: '#202124',
    fontSize: 17,
    fontWeight: '800',
    marginBottom: 13,
  },
});

export default styles;
